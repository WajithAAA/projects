# projects
all my projects and practice codes


get a pull
git pull

check status
git status

add updates
git add <folder name>

add all updates 
git add . 
  
commit commands
git commit -m "message here"

push updates
git push origin master
